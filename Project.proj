<Project>
  <PropertyGroup>
    <MajorVersion>1.0</MajorVersion>
    <BuildNumber>0</BuildNumber>
  </PropertyGroup>

  <PropertyGroup>
    <Version>$(MajorVersion).$(BuildNumber)</Version>
  </PropertyGroup>

  <Target Name="Build">
    <Message Text="Build version $(Version) from $(BranchName)" />
  </Target>
</Project>